[package]
name = "valence-encapsulation-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = "0.29.0"
solana-program-test = "1.17"
solana-sdk = "1.17"
spl-token = "4.0"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
valence-registry = { path = "../../programs/registry" }
valence-shard = { path = "../../programs/shard" }

[lib]
crate-type = ["cdylib", "lib"]